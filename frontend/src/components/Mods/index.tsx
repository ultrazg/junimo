import React from 'react'
import { ReadConfig } from '@/utils'
import styles from './index.module.scss'

const Mods = () => {
  const loadMods = () => {}

  return <div className={styles['mods-wrapper']}>mods layout</div>
}

export default Mods
